spring:
  datasource:
    url: 'jdbc:mysql://localhost:3306/weather_from_ya?useSSL=false&serverTimezone=UTC'
    username: bestuser
    password: bestuser
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect
    hibernate:
      ddl-auto: validate